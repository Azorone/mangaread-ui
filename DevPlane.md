# 需求

## 重构必须遵守
1. 必须保证项目截图、截图保存、截图后的图片拖拽功能正常运行
2. 漫画的持久化是按照导入的目录结构，进行移动，移动模式分为拷贝和移动。
拷贝模式是按照导入的漫画路径，进行漫画文件夹的路径进行复制，不对原路径的漫画进行任何修改、删除操作。
移动模式则是将需要导入的漫画移动到项目的mangaspace目录中的mangastore目录中。
3. 为项目添加配置文件
4. 将项目的分为启动阶段、运行阶段、结束阶段。在启动阶段加载项目的配置文件，config.json（该文件存储在项目）
5. 项目的截图目录、漫画存储目录、配置文件目录，统一存放在生产环境下，打包后的exe文件所在目录的同级目录mangaspace中。
比如打包后，mangaread.exe的路径为 D:/tool/mangared/magared.exe。则 截图目录存储在、漫画存储目录、配置文件目录，存储在D：tool/mangaspace目录中。注意：若不存在多级目录，则将mangaspace文件夹放在与mangared.exe同级目录中。
6. 项目重构后，必须保护项目的生命周期方法，方法名，以及作用。
7. 漫画的存储,采用json文件进行存储。共有以下三个josn文件，存储在mangaspace的mangastore中。
首先，需要知道mangastore的目录结构。
结构如下。

mangastore/
mangalist.json
history.json文件
-漫画名称一
    --->mete.json
    --->第一章
        ---->0001.png
        ---->0002.png
        .....
    --->第二章
        ---->0001.png
-漫画名称二
    --->第一章
-漫画名称三

若需要在界面加载漫画名称一，第一章，第一页图片。访问路径应该是 漫画名称一/第一章/0001.png.

json文件，mangalist存储在mangastore/mangalist.json
导入后，每一个本漫画在导入后会产生一个json对象。

{
    "manganame":漫画名称一
    “mete”：该漫画的mete.json所在目录
}
在magalist中的结构为
{
    mangalist:[
        {
    "manganame":漫画名称一
    “mete”：该漫画的mete.json所在目录
},      {
    "manganame":漫画名称二
    “mete”：该漫画的mete.json所在目录
}
    ]
}
同时，每一本漫画产生的mete.json。
结构如下：
{
    "manganame":漫画名称一,
    "importtime"：2026/5/22,
    "updatetime":2026/5/22,
    chapterlist:[
        {
            chaptername:"第一章",
            imagesize:1
            imgage:[
                name:"0001.png"
                url:"漫画名称一/第一章/0001.png"
            ]
        }
    ]
    chaptersize:1
}

当用户阅读后，会产生一个history.json文件。该文件记录了用户阅读某漫画后，最好一次离开的时的章节和对应的页面。在阅读历史中，点击后，便能直接进行跳转。
8. 项目重构后需要添加一下几个方法，并进行合理使用
initmanga（）：初始化项目，创建mangaspace文件，以及一些初次安装后，首次启动需要进行的一些工作。比如第5点需求。
loadmanga（）：加载项目，加载项目的配置文件、加载项目的漫画存储描述json文件，mangastore.json。加载项目的阅读历史文件。
exitmanga（）：项目退出前，执行的函数。该函数需清空项目截图文件夹和项目运行时，产生的一些临时文件。
importmanga(): 将项目的导入功能封装为一个函数，可在主进程中，被其它进程进行掉用。
exportmanga():将漫画以压缩后的zip压缩包方式进行导出。在其它进程中调用，避免占用electron的主进程。
###  添加漫画增删改查功能。

1. 选择漫画后，可为该漫画导入新章节，并将对应的json文件进行修改。
2. 选择漫画后的某一章，可为该漫画添加新的一页图片。并对对应的json文件进行修改。
3. 漫画名称可修改、漫画章节名可修改、漫画对应的图片名称可修改。漫画中对应的图片名称只支持修改为数字，比如0001修改为0002。修改名称时，需注意修改对应的json，需先保证磁盘中数据修改成功后，在修改对应的json。修改操作中，修改磁盘中存储的数据名称和修改json中的数据，必须为原子操作。修改前，需记录一份修改操作.json。修改操作的json名称为待修改的漫画的名称和时间戳，存储在mangaspace的temp目录中。修改失败，可根据该文件进行存储在磁盘中的漫画数据的名称，进行还原。
4. 查看功能，进行漫画图片的展示。展示功能项目以存在。请进行优化。
